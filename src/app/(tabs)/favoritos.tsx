import { PlaceholderScreen } from '@/components/PlaceholderScreen';

export default function FavoritosScreen() {
  return (
    <PlaceholderScreen
      title="Favoritos"
      description="Base preparada para guardar actividades favoritas."
    />
  );
}
